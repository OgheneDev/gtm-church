export default function MinistriesHero() {
  return (
    <section className="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 pt-16 pb-12 text-center animate-[settle_0.7s_ease-out_both]">
      <p className="text-[13px] font-medium text-(--gold) mb-4">Ministries</p>
      <h1 className="font-(family-name:--font-display) font-semibold text-[32px] md:text-[44px] leading-tight text-(--ink) mb-6">
        Find Your Place
      </h1>
      <p className="text-base leading-relaxed text-(--text-muted) max-w-[38em] mx-auto">
        Discover opportunities to grow, serve and build meaningful relationships
        within our church family.
      </p>
    </section>
  );
}
